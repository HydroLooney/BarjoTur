// Surface Worker C — front React (Vite + Tailwind + shadcn/ui + react-map-gl/MapLibre + zustand), voix R7.
// Ossature à écrire par C (T011) : entrée main.tsx, App, tokens de style, darkmode token-driven, PWA-ready ;
// reprise de la brique carte animée `fige.geom` (T012).
// Placeholder de fondation posé par le Maître pour garder le typecheck vert. Ne pas garnir ici.
export {};
