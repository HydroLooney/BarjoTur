{
  "name": "barjotur",
  "private": true,
  "version": "3.0.0",
  "description": "Barjøtur, le voyage qui vous ressemble. Compagnon d'un roadtrip familial en van en Norvège.",
  "type": "module",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "build": "npm run build --workspace shared && npm run build --workspace server && npm run build --workspace client",
    "typecheck": "tsc --noEmit -p shared && tsc --noEmit -p server && tsc --noEmit -p client",
    "lint": "eslint shared/src server/src client/src",
    "format": "prettier --write shared server client",
    "format:check": "prettier --check shared server client",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@types/node": "^22.9.0",
    "eslint": "^9.15.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.15.0"
  },
  "engines": {
    "node": ">=20"
  }
}
