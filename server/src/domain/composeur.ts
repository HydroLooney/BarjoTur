// Contrats composeur désormais CANONIQUES dans @barjotur/shared (posés par M, M015 puis étendus M062). Re-export.
// Le BFF reste un passe-plat du sidecar ; ComposeReponse.geom est fixé (animation C-11), `etapes` = EtapeRoutee[] (M062).

export type { ComposeInput, ComposeMeta, ComposeReponse, ArretImpose, EtapeRoutee } from '@barjotur/shared';
