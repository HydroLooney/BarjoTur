// Contrats composeur désormais CANONIQUES dans @barjotur/shared (posés par M, M015, commit 5543ba9). Re-export.
// Le BFF reste un passe-plat du sidecar ; ComposeReponse laisse geom/fige/etapes en `unknown` (détail non figé).

export type { ComposeInput, ComposeMeta, ComposeReponse } from '@barjotur/shared';
