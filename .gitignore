# dépendances
node_modules/
**/node_modules/

# build
dist/
build/
client/dist/
server/dist/
shared/dist/
*.tsbuildinfo

# env et secrets (jamais dans le repo, cf socle references/secrets.md)
.env
.env.*
!.env.example
secrets.env
**/secrets.env
.config/

# données lourdes et exports (vivent hors git, sur USB / OneDrive)
_exports/
_norvege_exports/
data/raw/
*.dump
*.sql.gz
*.gpkg
*.pmtiles

# python sidecar / calc
__pycache__/
*.pyc
.venv/
venv/

# système
.DS_Store
Thumbs.db

# échange interne (heartbeats vivants, journaux volumineux : suivis mais pas les gros logs)
00_Echange/**/*.lock

# données sources locales (lourdes, hors git)
data/

# artefacts de synchronisation DB1→DB2 (dumps dérivées + manifestes d'empreintes, transitoires par run)
db/sync/dumps/

# outillage et espaces de travail internes (hors dépôt) : configuration locale,
# échanges de coordination et notes de conception vivent en local, la doc publique est dans documentation/
.claude/
00_Echange/
docs/

# corbeille de reconciliation (jamais commitee)
.trash/
**/.trash/

# Captures de vérification Playwright (artefacts, hors repo)
client/captures-verif/
client/*.png

# Photothèque de dev, curation par POI (sources multiples A + M), pour revue et complément
# manuel de Guillaume. Volumineuse et à droits hétérogènes : JAMAIS poussée sur GitHub.
photos/
